import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class addlaber extends StatefulWidget {
  State<StatefulWidget> createState()=> _addlaber();
}
class _addlaber extends State<addlaber> {
  Widget build(BuildContext context) {
    return Scaffold(
        body: SingleChildScrollView(
          child: Column(),
        )
    );
  }
}